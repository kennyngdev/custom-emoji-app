apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: cogent-labs-assignment-default
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/cogent-labs-assignment-default: "true"
  podSelector:
    matchLabels:
      io.kompose.network/cogent-labs-assignment-default: "true"
